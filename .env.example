# ============================================================
# Skills Agentic Executor — Environment Variables
# ============================================================
# Copy this file to .env and fill in the values.
#   cp .env.example .env
# ============================================================

# ----------------------------------------------------------
# Required — OpenAI SDK
# ----------------------------------------------------------

# API key for OpenAI (or any OpenAI-compatible service)
OPENAI_API_KEY=sk-xxxxx

# ----------------------------------------------------------
# Optional — OpenAI SDK
# ----------------------------------------------------------

# Custom API base URL (for OpenAI-compatible services, e.g. Azure, local LLMs)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Model name to use (defaults to gpt-4o)
# OPENAI_MODEL=gpt-4o

# ----------------------------------------------------------
# Optional — LangSmith observability
# ----------------------------------------------------------

# Set to "true" to enable LangSmith tracing
# LANGCHAIN_TRACING_V2=true

# LangSmith API key (required when tracing is enabled)
# LANGCHAIN_API_KEY=lsv2_xxxxx

# LangSmith project name (defaults to "default")
# LANGCHAIN_PROJECT=skills-agent
